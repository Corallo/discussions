{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Package with scripts using in this repository.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc"
  },
  "dependencies": {
    "octokit": "2.1.0"
  },
  "devDependencies": {
    "@types/node": "20.4.9",
    "typescript": "5.1.6"
  }
}
