{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Package with scripts using in this repository.",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc"
  },
  "dependencies": {
    "octokit": "3.1.1"
  },
  "devDependencies": {
    "@types/node": "20.8.8",
    "typescript": "5.2.2"
  }
}
